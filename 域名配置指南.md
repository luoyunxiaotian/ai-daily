# ai-daynews.xyz 接入 Cloudflare Pages 配置指南

> 目标：让 `https://ai-daynews.xyz` 打开后内容和现在的 `https://ai-news-2026.pages.dev` 完全一致，
> 并去掉 `pages.dev` 字样。

## 当前状态（已确认）
- 域名已注册：到期 2027-08-23，注册商 = 阿里云 HiChina
- 当前 NS：`DNS15.HICHINA.COM` / `DNS16.HICHINA.COM`（阿里云默认）
- Cloudflare Pages 项目：`ai-news-2026`（域名 `ai-news-2026.pages.dev` 已在线）
- Cloudflare 账号：`2579984161@qq.com`（已登录）

---

## 第一步：Cloudflare 侧 — 添加自定义域（约 2 分钟）

1. 打开 https://dash.cloudflare.com/ 并登录
2. 左侧菜单 → **Workers 和 Pages** → 点 **ai-news-2026** 项目
3. 顶部标签 → **自定义域（Custom domains）**
4. 点 **「设置自定义域 / Set up a custom domain」**
5. 输入框填：`ai-daynews.xyz` → 点 **继续 / Continue**
6. 再填一次 `www.ai-daynews.xyz`（让 www 也指向站点）→ 继续
7. Cloudflare 会提示「去你的域名注册商修改 NS」——**先别急着改，它会给你两个 NS 地址，先复制下来**：
   - 形如 `xxx.ns.cloudflare.com` / `yyy.ns.cloudflare.com`
   - ⚠️ 这两个地址**每次添加都会不同**，以你屏幕上显示的为准

> 此步完成后，CF 会显示「待验证 / Pending」状态，这是正常的，因为 NS 还没改。

---

## 第二步：阿里云侧 — 修改 DNS 服务器（约 2 分钟）

1. 打开 https://dc.console.aliyun.com/ 并登录
2. 顶部 → **域名** → 找到 `ai-daynews.xyz` → 点 **「管理 / 解析设置」**
3. 找到 **「DNS 服务器 / 修改 DNS 服务器」** 入口（通常在域名信息页或解析设置页左侧）
4. 把当前的 `DNS15.HICHINA.COM` / `DNS16.HICHINA.COM`
   **替换为第一步拿到的两个 Cloudflare NS 地址**
5. 保存

> 阿里云改 NS 一般即时生效，但全球 DNS 传播需 **10 分钟 ~ 24 小时**（通常 1 小时内）。

---

## 第三步：等待生效 + 验证

改完 NS 后，用以下方式验证（或让我帮你跑命令）：

```bash
# 确认 NS 已指向 Cloudflare
dig NS ai-daynews.xyz

# 确认站点可访问（应返回 Cloudflare 的 IP）
dig A ai-daynews.xyz
curl -I https://ai-daynews.xyz
```

当 `dig NS` 输出里能看到 `ns.cloudflare.com` 时，Cloudflare 会自动完成 SSL 签发，
几分钟后 `https://ai-daynews.xyz` 就能打开。

---

## 第四步（可选）：把 pages.dev 设为重定向

等你确认 `ai-daynews.xyz` 完全正常后，可以在 CF Pages 项目设置里
把 `ai-news-2026.pages.dev` 配置为**重定向到** `ai-daynews.xyz`（避免两个地址并存）。
这一步不急，不影响使用。

---

## 注意事项
- ⚠️ `.xyz` 后缀**不能**做国内 ICP 备案（管局只认 `.com/.cn/.net` 等）。如果你以后要国内加速/备案，
  需另购 `.com` 或 `.cn` 域名。`.xyz` 用于海外/全球访问完全没问题。
- 改 NS 到 Cloudflare 后，**域名所有权仍归你**（在阿里云），Cloudflare 只管解析。
- 阿里云的实名认证信息不会暴露给 Cloudflare（隐私加分）。
